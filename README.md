# Galaxy Smoke Shop Website

Static Cloudflare Pages website.

## Deploy update
Copy the contents of this folder into your local GitHub project folder, then run:

```bash
git status
git add .
git commit -m "Update Galaxy Smoke Shop website"
git pull --rebase origin main
git push origin main
```

Cloudflare Pages will redeploy automatically from GitHub.
