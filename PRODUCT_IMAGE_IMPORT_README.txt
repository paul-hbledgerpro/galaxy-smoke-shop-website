Galaxy Smoke Shop — Product Image Import Notes

The website is ready to display product images directly inside each product card image slot.
Customer-facing image links are not shown.

To bulk-add AK Wholesale images, provide one of these:
1. CSV/XLSX export with product name or UPC and direct image URL.
2. ZIP folder of images named by UPC or exact product name.
3. AK product data export that includes SKU/model/product title and image URL.

The file ak-wholesale-image-import-template.csv contains every product currently shown on the website.
Fill ak_image_url with the direct image URL or send a completed wholesaler export with matching product names/UPCs.

Known product-name cleanup:
- Numeric/UPC display-name issues have been corrected in the website data. Example: SMOK Nord 2 rows now display product names instead of UPC-style numbers.
